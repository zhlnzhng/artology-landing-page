# Artology landing page

The public website for Artology, built with Jekyll and published through the repository's existing GitHub Pages setup.

## Local preview

```sh
bundle install
bundle exec jekyll serve
```

The App Store link is intentionally left blank until the Artology listing is available. Add it to `appstore_link` in `_config.yml`; the “Coming soon” labels will then become download buttons automatically.
